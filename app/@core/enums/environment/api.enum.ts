import { environment } from 'src/environments/environment';

export const API = {
    authentication: environment.urls.authenticationUrl,
    immunizationSystem: environment.urls.immunizationSystemUrl,
}
