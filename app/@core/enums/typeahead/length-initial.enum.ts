/**
 * Enum contendo valores iniciais para possiveis validações
 */
export enum InitialLength {
    InicialDefaultLength = -1,
    InicialLength = 0
}

/**
 * Enum contendo a quantidade de itens necessários para ativar a barra de rolagem de box de listagem
 */
export enum BoxList {
    ElementsVisibleBox = 3
}
