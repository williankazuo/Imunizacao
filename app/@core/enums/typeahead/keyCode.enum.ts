/**
 * Enum contendo as principais teclas do teclado para validação
 */
export enum KeyKode {
    KeyDown = 40,
    KeyUp = 38,
    KeyEnter = 13
}

/**
 * Enum para definir e quando usar algum tipo de scroll
 */
export enum KeyKodeScroll {
   ScrollUp = 1,
   ScrollDown = 0,
}